# GitHub Pages 部署配置
# 复制本文件为 .env 并填写你的实际值（本地开发用）
# 也可以在 GitHub 仓库 Settings → Secrets and variables → Actions → Variables 中配置同名变量（CI 构建用）

# 站点域名（RSS / sitemap / canonical 依赖它生成绝对链接）
# 用户站点：https://<username>.github.io
# 项目站点：https://<username>.github.io（注意：不包含仓库名，仓库名由 PUBLIC_BASE 提供）
PUBLIC_SITE=https://your-username.github.io

# 站点基础路径
# 用户站点（仓库名 = <username>.github.io）或使用自定义域名：留空或填 /
# 项目站点（普通仓库）：填 /<repo>，例如 /astro_blog
PUBLIC_BASE=/
